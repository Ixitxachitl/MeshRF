<UserControl x:Class="MeshtasticRF.App.Views.MapView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="#FF1B1B1B"
             ClipToBounds="True"
             Focusable="True">
    <Grid>
        <!-- Tiles + markers are added to this canvas in code-behind. -->
        <Canvas x:Name="MapCanvas"
                Background="#FF1B1B1B"
                MouseLeftButtonDown="OnMouseDown"
                MouseLeftButtonUp="OnMouseUp"
                MouseMove="OnMouseMove"
                MouseWheel="OnMouseWheel"
                MouseRightButtonUp="OnRightClick" />

        <TextBlock x:Name="AttributionText"
                   Text="© OpenStreetMap contributors  ·  right-click to set home"
                   IsHitTestVisible="False"
                   HorizontalAlignment="Right"
                   VerticalAlignment="Bottom"
                   Margin="0,0,4,2"
                   FontSize="10"
                   Foreground="#AAFFFFFF" />

        <Border HorizontalAlignment="Left" VerticalAlignment="Top"
                Margin="8" Padding="2"
                Background="#CC202020" CornerRadius="3">
            <StackPanel Orientation="Vertical">
                <Button Content="+" Width="28" Height="28" FontSize="16"
                        Click="OnZoomIn" ToolTip="Zoom in" />
                <Button Content="&#x2013;" Width="28" Height="28" FontSize="16"
                        Margin="0,2,0,0"
                        Click="OnZoomOut" ToolTip="Zoom out" />
                <Button Content="&#x2302;" Width="28" Height="28" FontSize="14"
                        Margin="0,2,0,0"
                        Click="OnRecenter" ToolTip="Fit all markers" />
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
